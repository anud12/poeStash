<div class="stash">
	<% tab.stash.items.forEach(function (item) { %>
		<img class="item"
			 style="left: calc((47.5px * <%= item.x %>));
					 top: calc((47.5px * <%= item.y %>));"
			 src="<%= item.icon %>"/>
	<% }) %>
</div>